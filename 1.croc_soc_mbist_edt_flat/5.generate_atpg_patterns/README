Generate and simulate ATPG patterns. Scan insertion done using Tessent Scan or a third-party scan insertion tool.

Script to use if scan was inserted using Tessent Scan
    1.run_stuck_tessent_scan
    2.run_transition_tessent_scan
    3.run_sims

Script to use if scan was inserted using a third-party scan insertion tool
    a.run_stuck_third_party
    b.run_transition_third_party
    c.run_sims

Log files are stored in the "logfiles" directory

Patterns are stored in the "patterns" directory

