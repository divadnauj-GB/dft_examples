
Script to use: run_insert_occ_edt
Log file:  logfiles/run_insert_occ_edt.log

2.insert_occ_edt - The script run_insert_occ_edt is used to insert EDT and OCC inside the design.

            During create_dft_specifcation point to the existing TAP controller from Step 1. 
            In the DFTSpec for EDT, specify "connect_bscan_segments_to_lsb_chains: On". This will
            automatically connect the segmented boundary scan chains to EDT.
            The scan_chain_count number is specified for just the functional logic chains.
            The EDT will be automatically added with the number of chains required for segmented boundary scan chains.

